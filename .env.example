NEO4J_URI=
NEO4J_USERNAME=
NEO4J_PASSWORD=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Powers ml-services' POST /chat (AI Health Assistant) and nothing else.
# Get a free key at https://aistudio.google.com/apikey — without it, the
# assistant returns an honest "not configured" message instead of
# failing silently.
GEMINI_API_KEY=